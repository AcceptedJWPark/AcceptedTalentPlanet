<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.accepted.acceptedtalentplanet">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoadingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".JoinGenderActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".RegistEmailActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".RegistPasswordActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".RegistNameActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".RegistGenderActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".RegistBirthActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".RecyclerActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MyprofileActivity" />
        <activity android:name=".MytalentActivity" />
        <activity android:name=".TsharingActivity" />
        <activity android:name=".TalentStateActivity" />
        <activity android:name=".MessageBoxActivity" />
        <activity android:name=".CustCenterActivity"></activity>
    </application>

</manifest>